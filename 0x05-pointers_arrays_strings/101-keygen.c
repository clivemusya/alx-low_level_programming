#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Generates random valid passwords
 *
 * Return: 0
 */

int main(void)
{
	char password[101];
	srand(time(0));
	printf("%s", password);
}
